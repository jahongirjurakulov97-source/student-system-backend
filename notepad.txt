const express = require("express");
const app = express();

app.get("/", (req, res) => {
  res.send("Server ishlayapti 🚀");
});

app.listen(5000, () => {
  console.log("Server 5000 portda ishlayapti");
});